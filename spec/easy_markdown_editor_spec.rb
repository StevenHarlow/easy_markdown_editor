require "spec_helper"

RSpec.describe EasyMarkdownEditor do
  it "has a version number" do
    expect(EasyMarkdownEditor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
